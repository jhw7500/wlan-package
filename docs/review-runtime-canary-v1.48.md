# Review runtime canary v1.48

This temporary pull request verifies that the Claude, Gemini, and OpenCode
review workflows pinned to automation v1.48 can invoke their providers from an
ordinary pull request after the workflow rollout reached the default branch.

The pull request will be closed without merging after the runtime evidence is
recorded.
