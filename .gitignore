release/
dist/wlan/usr/local/wlan-bridge/
dist/wlan/usr/local/etc/
__pycache__/

.gemini/
gha-creds-*.json

tmp/
.worktrees/
docs/01-plan/
docs/02-design/
docs/04-report/
docs/05-runbook/
# Superpowers spec/plan은 로컬 작업 산출물 — 확정 계약은 README/CHANGELOG/정식 가이드/테스트에 유지
docs/superpowers/
network.json
.base/
.claude/
.paul/

# Tool directories
.bkit/
.omc/
.omo/
.omx/
.serena/
.vscode/
.context/
dist/wlan/usr/local/scripts/.omc/
.gstack/

# 빌드 산출물 — wlan-driver-v2(mlanutl, *.ko) / wlan-opc(opcd, vhlctl)에서 빌드
# 배포는 빌드 파이프라인이 dist에 staging (git 추적 제외)
dist/wlan/opt/wlan/bin/mlanutl_*
dist/wlan/opt/wlan/driver/*.ko
dist/wlan/opt/wlan/driver/debug/*.ko
dist/wlan/usr/local/opc/bin/

# 특허 출원 전 자기공개 방지 — 발명/특허 문서는 PUBLIC repo에 커밋 금지 (2026-06-24)
# wlan-package는 PUBLIC이므로 아래 문서 커밋 시 신규성 상실 위험
docs/04-report/wbridge-patent-prior-art-*.md
docs/04-report/wbridge-invention-disclosure-*.md

# 로컬 툴/에이전트 세션 artifact (추적 제외)
.playwright-mcp/
.superpowers/
artifacts/

# 세션 인계 문서 — 다음 세션이 읽을 때까지만 가치가 있다. 코드와 무관한 서술이
# 히스토리에 쌓이는 것을 막는다. 완료되면 내용을 PR 본문/Notion 으로 옮기고 파일은 지운다.
# 슬래시 없는 패턴이라 .jhw/HANDOFF-*.md 같은 하위 경로도 함께 잡는다.
# 예외: 담당자·팀에게 넘기는 '경계 인계'는 docs/ 에 커밋한다
#       (예: docs/wifi_init_conf_webui_handoff.md — 이 패턴에 걸리지 않는다).
HANDOFF*.md

# 세션 작업 디렉터리 — 인계 문서, 일회성 검증 스크립트 등
# verify-pr134-target.sh 는 PR #134 에 딸려 들어간 실수 커밋이라 이 PR 에서 untrack 했다
# (커밋 본문이 로밍 데몬 변경만 서술하고 이 파일을 언급하지 않는다). 파일은 디스크에 남는다.
.jhw/

# 빌드 산출물 — wlan-driver-v2(mapp/mlanevent)에서 빌드 (mlanevent.exe 는 벤더 제공이라 추적 유지)
dist/wlan/opt/wlan/bin/mlanevent_*
