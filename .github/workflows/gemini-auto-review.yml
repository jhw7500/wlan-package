name: Gemini Auto PR Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  gemini-review:
    permissions:
      contents: 'read'
      pull-requests: 'write'
      issues: 'write'
      id-token: 'write'
    uses: 'jhw7500/automation/.github/workflows/gemini-auto-review.yml@v1.9'
    with:
      pr_number: '${{ github.event.pull_request.number }}'
    secrets: 'inherit'
