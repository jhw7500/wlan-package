name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  claude-review:
    if: ${{ github.event.pull_request.head.repo.fork == false && github.event.pull_request.head.repo.full_name == github.repository }}
    permissions:
      contents: read
      pull-requests: write
      issues: read
      id-token: write
    uses: 'jhw7500/automation/.github/workflows/claude-code-review.yml@v1.15'
    with:
      pr_number: '${{ github.event.pull_request.number }}'
      force_run: ${{ github.event.pull_request.head.repo.fork == false && github.event.pull_request.head.repo.full_name == github.repository }}
    secrets: 'inherit'
