## 변경 요약

<!-- 한 줄. 무엇을 왜 바꿨는지. -->

## 변경 범위

- [ ] 코드 (스크립트·데몬·라이브러리)
- [ ] 설정·스키마 (`wifi_init_conf.json`, `wifi_init_conf.schema.json`)
- [ ] 문서
- [ ] 패키지 메타 (`DEBIAN/control`, 버전 자리)
- [ ] 드라이버 페이로드 (`DRIVER_MANIFEST.md` / component lock)

## 근거

<!-- 주장마다 근거의 성격을 밝힌다. 측정·확인·추정을 섞지 않는다. -->

- **측정한 것**:
- **추정한 것** (측정 안 함):
- **검색 범위** <!-- 부정·전체 주장("유일하다"/"없다"/"N건이다")을 했다면 무엇을 어디까지 검색했는지 -->:

<!-- 실기 측정이 있으면: -->
- 예측·허용오차를 **실행 전에** 등록했는가:
- 계기 게이트(잡음 대비 신호 분해 가능 여부)를 확인했는가:
- 같은 설정의 반복으로 산포를 추정했는가:

## 타겟 검증

- [ ] 온타겟 확인 완료 <!-- 대상 장비 / 접속 경로 -->
- [ ] 변경한 상태를 원복하고 독립 검증했다 <!-- 유닛 수, drvdbg, DropInPaths, config sha -->
- [ ] 해당 없음 (실기 무관)

## 릴리스 게이트

- [ ] `python3 scripts/gen_config_defaults.py --check` <!-- 설정·스키마·handoff 3축 -->
- [ ] `scripts/validate_release.sh pre`
- [ ] 버전 자리 판단 <!-- 올렸는가 / 올리지 않은 근거(해당 .deb 미빌드 등) -->
- [ ] 해당 없음

## 기존 기기 영향

<!-- 설정 기본값을 바꿨다면 필수. postinst 의 JSON deep merge 는 "기존 키는 사용자 값 유지"
     이므로 이미 그 키를 가진 기기에는 새 기본값이 내려가지 않는다. 신규 설치·factory reset
     한정인지, 마이그레이션을 넣었는지, 넣지 않았다면 그 근거를 적는다. -->

## 리뷰 관점

<!-- "리뷰해줘" 대신 어디를 봐야 하는지 지정한다. 이 저장소에서 실제로 결함이 나온 곳:
     문서 산문의 사실 주장, 정정이 원래 주장의 모든 위치에 적용됐는지, 측정 범위를 넘는 단정,
     diff 밖 파일에 남은 같은 주장. -->

1.
2.

## 한계·미검증

<!-- 이 PR 이 답하지 못한 것. 비워 두지 말고 "없음"이라도 적는다. -->
